apiVersion: v1
kind: PersistentVolume
metadata:
  name: recargos-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/recargos-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recargos-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recargos-db-deployment
  labels:
    app: recargos-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: recargos-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: recargos-db-deployment
        tier: database
    spec:
      containers:
        - name: recargos-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: SECRET_DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: SECRET_DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: recargos-db-data
      volumes:
        - name: recargos-db-data
          persistentVolumeClaim:
            claimName: recargos-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: recargos-db-service
  labels:
    app: recargos-db-service
    tier: database
spec:
  ports:
    - port: 5434
      targetPort: 5432
      protocol: TCP
  selector:
    app: recargos-db-deployment
    tier: database
  type: LoadBalancer